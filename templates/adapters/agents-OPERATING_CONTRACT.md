# Neutral contract — legacy `.agents/` path

This file exists so older templates and commands that reference **`.agents/OPERATING_CONTRACT.md`** resolve to the same **thin adapter** model as **`.agent/`**.

**Canonical neutral docs (read these):**

- **`.agent/operating-contract.md`** — full operating contract  
- **`.agent/runtime.md`** — map: `.claude/` vs adapters  
- **`.agent/handoff.md`** — prime → absorb → digest  

**Operational truth** remains only under **`.claude/`**. Do not treat `.agents/` as a second runtime.
