# Never commit
# Local workspace context (generated from OS_WORKSPACE_CONTEXT.template.md)
/OS_WORKSPACE_CONTEXT.md

# Local operator journal (generated from OPERATOR_JOURNAL.template.md)
/OPERATOR_JOURNAL.md

# Local validation / runtime evidence (opt-in writers only)
/logs/
/logs/*.jsonl
/logs/*.log
/local-evidence/
/evidence/local/
/exports/

# Local distribution zip + staging (build-distribution.ps1)
/dist/

# Generated skill mirrors (canonical is source/skills/); ignore if tooling materializes these under repo root
/.claude/skills/
/.cursor/skills/
/.codex/skills/

# Nested copy/clone of this repo at workspace root (common on Windows); do not track.
/claude-operating-system/

# Optional local clone of another fork for comparison (not part of the OS contract).
/extern-reference/

# Local scratch / backup scripts (never track)
*.local-backup.ps1

.env
.env.*
*.local
settings.local.json
*.log
.DS_Store
.claude/*.tmp
.claude/.simulation-delta-*.json
.claude/invariant-report.json
.claude/risk-model.json
.claude/semantic-diff-report.json
.claude/learning-loop-report.json
.claude/policy-audit-report.json
.claude/compliance-report.json
.claude/subgraph-index.json
.claude/simulation-report.json
.claude/contracts/.last-compare.json
.claude/invariant-lifecycle-report.json
.claude/coordination-report.json
.claude/epistemic-report.json

# OS invariant-engine dev deps (bundled dist is committed)
templates/invariant-engine/node_modules/
