# Advanced bootstrap example (reference)

Same intent as `../minimal/`, plus artifacts that appear when you pass **`-Profile node-ts-service`** or **`-Profile react-vite-app`** to `init-project.ps1` (stack profile file) and when **invariant-engine** bundles exist in the OS clone at init time.

Nothing here is user-specific or secret-bearing.
