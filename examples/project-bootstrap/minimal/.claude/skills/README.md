# Skills under `.claude/skills/`

Each subdirectory is one skill copied from `source/skills/<id>/` in the Claude OS repo (e.g. `bootstrap-governance/SKILL.md`).

Skills are **generated artifacts** for the project; canonical definitions stay in the OS `source/skills/` tree. Regenerate with `sync-skills` workflows when the OS repo updates.
